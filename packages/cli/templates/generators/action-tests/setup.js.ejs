'use strict';
const path = require('path');

// Shared configuration for action tests
const PROJECT_ROOT = path.join(__dirname, '../..');

// Default test users — generated by `capx add mocked-auth`
const ADMIN_USER = { username: 'admin_user', roles: ['admin'] };

module.exports = { PROJECT_ROOT, ADMIN_USER };
